#!/usr/bin/env python3
""" Module with a floor conversion function. """


def floor(n: float) -> int:
    """
    Convert a float to its floor value as an integer.

    Args:
        n (float): the floating-point number to convert.

    Returns:
        int: the floor value of the input float `n` as an integer.
    """
    return (int(n))
